//! Complication locations
module ComplicationLocation {
    enum Value {
        TOP = 1,
        BOTTOM
    }
}

//! Watch face styles
module Styles {
    enum Value {
        HOUR = 1,
        HOUR_MINUTES,
        HOUR_MINUTES_SECONDS
    }
}
